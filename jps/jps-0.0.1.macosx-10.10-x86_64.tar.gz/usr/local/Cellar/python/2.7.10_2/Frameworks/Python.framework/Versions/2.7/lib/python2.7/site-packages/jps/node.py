import zmq

class Node(object):
    def advertise(self, topic_name):
        return Publisher(self._context, topic_name, self._master_host,
                         self._master_pub_port)

    def subscribe(self, topic_name):
        return Subscriber(self._context, topic_name, self._master_host,
                          self._master_sub_port)

